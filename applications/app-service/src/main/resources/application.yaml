##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: crediya-authentication-service
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
